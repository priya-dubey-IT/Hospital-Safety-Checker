            </div>
        </div>

        {selectedAssignment && (
            <ReportEditor 
                assignmentId={selectedAssignment}
                onClose={() => setSelectedAssignment(null)}
            />
        )}
    </div>
    );
};

export default Dashboard;
